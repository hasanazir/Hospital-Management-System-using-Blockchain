// src/components/Dashboard.js
import React from 'react';

const Dashboard = ({ web3, account }) => {
    return (
        <div>
            <h2>Dashboard</h2>
            {/* Logic for accessing patient records, sharing data, etc. */}
        </div>
    );
};

export default Dashboard;
